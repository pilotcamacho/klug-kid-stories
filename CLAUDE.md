# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

**Klug Kid Stories** is a language learning web app built around spaced repetition and story-based contextualization. The core premise:

- Vocabulary progress is tracked per **word meaning** (not per word form). Each entry represents one specific semantic sense of a word.
- Words are always stored in their **base/lemma form** (no declination, conjugation, or inflection).
- The app is **language-agnostic** — designed to support any language pair.
- Students are evaluated by **typing answers**, never by self-assessment.

---

## Tech Stack

| Layer | Technology |
|---|---|
| Frontend | Next.js 16 (React, App Router) |
| Auth | AWS Cognito (via Amplify Gen 2) |
| Database | Amazon DynamoDB (via Amplify Gen 2) |
| Backend/Hosting | AWS Amplify Gen 2 |
| AI | Claude API (Anthropic) |

**Why Next.js:** AWS Amplify Gen 2 is React/Next.js-first. Amplify's libraries (`aws-amplify`), UI components (`@aws-amplify/ui-react`), and Cognito `Authenticator` are optimized for this stack.

---

## Commands

```bash
# Install dependencies
npm install

# Start local dev server (Turbopack)
npm run dev

# Start Amplify sandbox (generates amplify_outputs.json, required before running the app)
npx ampx sandbox

# Build for production
npm run build

# Lint
npm run lint
```

`amplify_outputs.json` is generated by `npx ampx sandbox` (local) or by the Amplify CI/CD pipeline (production). It is git-ignored and must never be committed.

---

## Project Structure

```
amplify/
  backend.ts          # Amplify backend entry point (auth + data)
  auth/resource.ts    # Cognito configuration
  data/resource.ts    # DynamoDB schema (TypeScript-first)
app/
  (app)/              # Route group: all authenticated routes
    layout.tsx        # Auth gate (Authenticator) + NavBar
    dashboard/
    vocabulary/
    review/
    settings/
  components/
    AmplifyProvider.tsx  # Configures Amplify + provides Authenticator context
    AuthProvider.tsx     # Authenticator modal wrapper for protected routes
    NavBar.tsx           # Top navigation bar
  layout.tsx          # Root layout (wraps everything in AmplifyProvider)
  page.tsx            # Redirects / to /dashboard
```

---

## Core Concepts

### Spaced Repetition

The app implements a spaced repetition system (SRS) to schedule word reviews:

- Each word meaning has a **retention score** — an estimate of how far into the future the student will still remember it.
- Review intervals are computed from a **forgetting curve model** fitted to:
  - The individual student's historical recall performance.
  - Aggregate data from other users (for cold-start and calibration).
- Correct answers push the next review further into the future; incorrect answers shorten the interval.
- The scheduler surfaces word meanings that are approaching the predicted forgetting point.

### Story-Based Contextualization

Words are never asked in isolation. The system presents the student with a **short story containing blanks**:

- Each story is generated using **only words the student has already learned** (words with sufficient retention score), ensuring full comprehension of the surrounding context.
- One or more blanks correspond to the word meanings being tested in that session.
- The student types the missing word(s) to complete the story.
- Answer evaluation accepts valid inflected forms (conjugations, plural, case, etc.) of the correct lemma.

### Word Entry

Students can add vocabulary in three ways:
1. **Pre-loaded** — vocabulary that has been preloaded, organized by frequency in the language. So that if the student starts from scratch, they will start learning the most used words.
2. **Manual entry** — the student defines a word meaning directly.
3. **Text import** — the student pastes a text; the system extracts, lemmatizes, and deduplicates the vocabulary for review and addition.

---

## Data Schema

Defined in `amplify/data/resource.ts` using the Amplify Gen 2 TypeScript schema API.

| Model | Purpose |
|---|---|
| `WordMeaning` | One row per semantic sense of a lemma. Owned by creator; shared words are readable by all authenticated users. |
| `UserWordProgress` | Per-user SRS state for a word meaning: `retentionScore`, `nextReviewAt`, review counts. Owner-only. |
| `ReviewEvent` | Immutable log of each review attempt. Used to train/refine the forgetting curve. Owner-only. |
| `Story` | A generated review story. Stores content (blanks as `___`) and target word meaning IDs. Owner-only. |

---

## AI Integration

AI (Claude API) is used in the following parts of the platform:

| Feature | AI Role |
|---|---|
| **Story generation** | Given the set of known words and a target word to test, Claude generates a short, coherent story using only allowed vocabulary, with the target word replaced by a blank. |
| **Word extraction from text** | When a student imports a text, Claude extracts distinct word meanings, lemmatizes them, and disambiguates multiple senses of the same word form. |
| **Answer evaluation** | Claude determines whether a typed answer is an acceptable form of the correct lemma (handling conjugation, declination, and typos within tolerance). |
| **Forgetting curve personalization** | A model uses the student's recall history to adjust interval predictions beyond the baseline SRS formula. |
| **Word meaning definition** | When a student adds a new word, Claude suggests a concise definition and example sentences in the target language. |

---

## Infrastructure

- **AWS Amplify Gen 2** manages provisioning of Cognito (auth) and DynamoDB (data).
- Environment variables for the Claude API key and Amplify config are managed via Amplify environment/secrets — never committed to the repo.
- Authentication flow uses the Amplify `Authenticator` React component.

---

## Implementation Phases

### Phase 1 — Foundation (complete)
Project scaffolding (Next.js + Amplify Gen 2), Cognito authentication (sign up, sign in, sign out), DynamoDB schema design (users, word meanings, review history), and basic UI shell with navigation.

### Phase 2 — Vocabulary Management
Pre-loaded frequency word lists (importable by language), manual word meaning entry, vocabulary browser with CRUD operations.

### Phase 3 — Proprietary SRS Algorithm Design & Implementation
Design and implement the proprietary forgetting curve algorithm. The algorithm must:
- Predict per-student, per-word-meaning forgetting time.
- Bootstrap from aggregate data across all users (cold start).
- Refine predictions continuously from the individual student's recall history.
- Expose a scheduling interface that returns the next review date for a given word meaning.

This phase is design-first: the algorithm must be specified and validated before implementation begins.

### Phase 4 — Core Review Sessions
Typed-answer review sessions driven by the Phase 3 scheduler. Exact-match answer evaluation (no AI yet). Retention score updates after each answer.

### Phase 5 — Story-Based Review (AI)
Claude API integration. Story generation constrained to the student's known vocabulary, with target words replaced by blanks. Replaces plain review sessions from Phase 4.

### Phase 6 — Text Import (AI)
Paste-a-text flow: Claude extracts and lemmatizes vocabulary from the pasted text, disambiguates multiple senses, and presents the student with a review/add word list.

### Phase 7 — AI-Enhanced Evaluation & Definitions
Claude-powered answer evaluation (accepts valid inflected forms, typo tolerance), word definition and example sentence suggestions on manual entry, and forgetting curve personalization using the student's accumulated history.

### Phase 8 — Analytics & Polish
Student progress dashboard, vocabulary statistics, mobile responsiveness, and performance tuning.
